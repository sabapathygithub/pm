[project]
name = "pm-backend"
version = "0.1.0"
description = "FastAPI backend for PM MVP"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.115.12",
  "httpx>=0.28.1",
  "pytest>=8.3.5",
  "uvicorn[standard]>=0.34.0",
]

[tool.uv]
package = false
